include $(top_srcdir)/Makefile.base

EXTRA_DIST = themerc radio.xpm

themedir = $(METATHEMEDIR)/themes/redmond
theme_DATA = themerc
theme_LTLIBRARIES = libmt_redmond.la

libmt_redmond_la_SOURCES = redmond.c
libmt_redmond_la_LDFLAGS = -avoid-version -module
